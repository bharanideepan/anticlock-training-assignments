{
  "name": "crm-app",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  },
  "scripts": {
    "test": "pnpm --filter backend test && pnpm --filter frontend test",
    "test:e2e": "pnpm --filter e2e test",
    "test:cov": "pnpm --filter backend test:cov && pnpm --filter frontend test:coverage",
    "lint": "pnpm --filter backend lint && pnpm --filter frontend lint",
    "typecheck": "pnpm --filter backend typecheck && pnpm --filter frontend typecheck",
    "build": "pnpm --filter backend build && pnpm --filter frontend build",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md,css,scss}\" --ignore-path .prettierignore",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md,css,scss}\" --ignore-path .prettierignore"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "bcrypt",
      "prisma"
    ]
  }
}
